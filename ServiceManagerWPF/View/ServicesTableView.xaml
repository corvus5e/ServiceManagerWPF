<UserControl x:Class="ServiceManagerWPF.View.ServicesTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ServiceManagerWPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid IsReadOnly="True" 
                  Name="_servicesDataGrid" 
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Services}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DisplayName}" Width="2*" Header="Name"/>
                <DataGridTextColumn Binding ="{Binding Status}" Width="*" Header="Status"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
